# Plano: Melhorias no Mapa da Campanha

## 1. Verificar/Corrigir dimensões do mapa vs coordenadas dos POIs
- Descobrir dimensões reais de `public/map/mapa.png`
- Ajustar `MAP_WIDTH` e `MAP_HEIGHT` em `MapCanvas.tsx` se necessário
- Verificar se as coordenadas dos POIs em `pois.ts` estão dentro das dimensões corretas

**Arquivos**: `src/components/MapCanvas.tsx`, `src/data/pois.ts`

## 2. Debounce no resize do mapa
- Adicionar debounce de ~150ms no listener de resize em `MapCanvas.tsx` para evitar recálculos excessivos em mobile

**Arquivos**: `src/components/MapCanvas.tsx`

## 3. Header auto-hide em zoom profundo
- Esconder o header quando o zoom ultrapassar um threshold (ex: 2x do fitScale)
- Usar CSS transition para animação suave

**Arquivos**: `src/components/MapCanvas.tsx`, `src/App.tsx`, `src/styles.css`

## 4. Loading indicator para o mapa
- Adicionar um estado de carregamento na imagem do mapa
- Mostrar um spinner ou skeleton enquanto `mapa.png` carrega

**Arquivos**: `src/components/MapCanvas.tsx`, `src/styles.css`

## 5. Trap focus no modal POICard
- Implementar focus trap dentro do dialog para que Tab cycling fique contido no modal

**Arquivos**: `src/components/POICard.tsx`

## 6. Marker escala inversamente ao zoom
- Ajustar o tamanho dos marcadores proporcionalmente ao zoom para manter tamanho visual constante

**Arquivos**: `src/components/POIMarker.tsx`, `src/components/MapCanvas.tsx`

## 7. Testes básicos
- Configurar Vitest
- Testes de render dos componentes principais (MapCanvas, POIMarker, POICard)

**Arquivos**: Novos arquivos de teste, `vitest.config.ts`, `package.json`

## Ordem de execução
1. Verificar dimensões do mapa (pré-requisito para validar coordenadas)
2. Implementar debounced resize
3. Loading indicator (simples, rápido)
4. Header auto-hide
5. Marker scale inverso ao zoom
6. Focus trap no modal
7. Testes
